import { CreateRefreshTokensDto } from './create-refresh-tokens.dto';

describe('CreateRefreshTokensDto', () => {
  it('should be defined', () => {
    expect(new CreateRefreshTokensDto()).toBeDefined();
  });
});
